import { Routes } from '@angular/router';
import { carritoCompraComponent } from './pages/carrito-compra/carrito-compra.component';

export const routes: Routes = [
  { path: '', component: carritoCompraComponent}
];
